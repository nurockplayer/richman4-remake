# 研究所建設與生產

Issue #39 在完整來源圖新局啟用 `original_research`，使用 v12；舊存檔 v1–v11 保留其原規則。
研究所是商業設施類型 4，正常建造一級、最高五級。正常升級、工人、天使、福神與建設公司
沿用各自的既有適用條件、價格與上限。改建與換屋也能處理研究所。

## 選擇與交付

擁有者實際落地到已建研究所，或在那次造訪建成研究所後，可以選擇不高於設施等級的產品。
研究所查封或玩家無法行動時不能選擇；漲價不阻擋選擇。每次造訪最多成功選擇一次，也可略過。
選擇不花費現金、卡片或共享工具庫存；再次造訪可更換產品並重新開始倒數。

| 研究所等級 | 解鎖產品 | 工具來源 ID |
| --- | --- | --- |
| 1 | 機器工人 | 9 |
| 2 | 時光機 | 10 |
| 3 | 傳送機 | 11 |
| 4 | 工程車 | 12 |
| 5 | 核子飛彈 | 13 |

選定後倒數五次自己的回合，選定的當回合不扣一次。每次切換到擁有者的新回合時遞減，
第五次交付一個工具。多入口設施只處理一次；其他玩家的回合不影響這項倒數。
玩家住院／服刑與設施查封不停止既有生產倒數。持有工具已達九個時，交付不增加數量，該次生產結束而不重試。

## 狀態與相容

每筆實體設施保存 `research_tool`（0 表示尚未選擇，1–5 對應產品）與 `research_turns`（0–5）。
所有道路入口保持相同值。換屋或改建只更動建物類型／等級，生產狀態留在原址。
非研究所不處理倒數；若研究所等級已低於產品要求，下一次輪到擁有者時取消倒數。
因此，降級後等待取消的狀態仍是合法存檔，不能在載入時任意改寫。

所有權轉移後由新擁有者的回合處理。沿用既有清算流程收回土地時，清除所有權與建物，生產狀態留在原址；
無主設施不處理倒數。這項保留策略未宣稱是完整原版破產介面的精確還原。

AI 選擇當時等級所允許的最高階產品。工具 10–13 在此切片可生產、持有與存檔，
其使用效果仍待後續接入；背包明示未還原。原版生產視窗與動畫尚未還原。

## 已核對來源

依擁有者 pinned MultiverseJourney EXE（SHA256 見 `original-facilities.md`）直接核對：

- `41a1f2..41a2ae` 建造；`41a2b3..41a2c8` 正常升級依 `474940` 上限表，研究所為五級。
  `40b1f9..40b212` 通用建設也使用相同上限，沒有類型 4 特別禁止。
- `41b111` 前的落地尾段限制擁有者、玩家狀態、已建研究所與低狀態位元組，才呼叫 `44101d`。
  `4411ed..4411fb` 取消不變，合法選擇寫產品並把倒數設為五。
- `418f93..419006` 先選下一位玩家，`419033..41903e` 再呼叫生產／回合處理。
  `41cd97..41ce33` 逐設施檢查類型、倒數、擁有者與等級，歸零時以產品加八呼叫 `445a4d`。
- `445a4d..445aa1` 限持有九個；來源 ID 大於八不扣共享庫存。
  `40b89e..40b8b3` 換屋只交換類型、等級，`4431e4..4431fe` 改建同樣不搬動生產欄位。
