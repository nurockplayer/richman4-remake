# Source setup surface

Issue #115 / parent #102 S02–S03. The source top frame is440×155 at(4,10), the side frame192×461 at(445,10), over the complete640×480 map background. These are logical coordinates, independent of output pixels. Portrait hit cells start(8,15), six columns/two rows of72. The four map rows and OK/EXIT use the source callback bounds. Source asset labels are not duplicated; separate configuration field labels are drawn because the raw side frame does not contain them.

Portrait clicks append humans in click order; clicking an already selected portrait removes it and compacts the order. A game needs at least one selected human. There are no invisible player-slot or human/AI controls. On confirmation a local RNG initialized from the resolved new-game seed fills the remaining unique unused characters as AI, preserving the running match RNG until adoption. This is deterministic seed-based reproduction, not a claim of bit-identical original libc RNG. Chosen vehicle refreshes all selected human previews; source anchors are x[330,110]/[366,220,74]/[385,275,165,55] for2/3/4 players and y440. The visible map check uses source atlas chunk8 (chunk7 is a dropdown table, not a selection mark). Selected portraits are visibly dimmed as in the source draw path.

Game uses four maps. MultiverseJourney has two stages of four, with background index stage*4+map and side atlas chunk stage*20+1. Stage/edition must arrive from the upstream title/menu entry; source setup does not invent another stage switch. That S01 title/stage integration is still pending. Missing source assets use bounded fallback geometry; unknown assets do not become fidelity evidence. The pinned source uses baked Chinese level labels, whereas some external Game screenshots show English map names; the pinned edition atlas is retained and the external variant is not blended into it.

New-game source definitions are recovered from catalog identity and matching source metadata. A running snapshot supplies geometry for display but lacks initial stock/company/capability records. The source OK callback merges player choices with capabilities from the validated catalog before the strict factory. It therefore cannot silently recreate a three-stock game from the panel's presentation-only options. Land tenure uses reviewed #120; source choices, initial date, human controls and vehicle survive restart defaults. Exit returns to the title or current game according to entry intent, preserving the current match.

Evidence: test-onlycc73bf9 interaction RED18/14 before the repair, then23/0 after; an earlier test syntax error is not qualifying evidence. The separate immutable factory test0961b8f exposes missing source capabilities: synthetic RED18/4, real twelve-map catalog RED18/8, then18/0 with the source OK callback. Source UI integration52/0 on the fixture and actual catalog, setup controls96/0 and land194/0 are checked separately. The revised older integration test replaces the disproven per-slot model/coordinates/unsupported-land assumptions; cancel, inventory, vehicle and bank assertions remain. Tests use no owner audio preferences. These checks and component renders do not replace ordinary physical-input/package or independent visual acceptance.
